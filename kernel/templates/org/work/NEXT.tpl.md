---
summary: L-scale ordered queue template, the plain list PLAN maintains at triage
type: template
tags: [eos]
template: true
---

# NEXT

The ordered work queue, L scale. A plain list PLAN rewrites at triage;
WORK sessions take from the top unless their launcher names a work
order. Items reference `org/work/items/WO-####-<slug>.md` files once
they exist. Priority and claims live on the orders themselves.

1. Run PB-001 Genesis (PLAN). No work-order file exists for this item;
   Genesis is the session that creates the work-order system and
   rebuilds this list, foundation items first.

---
summary: Herbfield Lane task list, the S-scale work surface, open items, questions and log
type: template
tags: [eos]
compiled_from: kernel/templates/TASKS.tpl.md
---

# TASKS

The venture's work surface at S scale, hand-kept because S runs
without the integrator tooling. Express work needs no row here; the
commit message is the whole record. A Standard task gets one row
while open and one log line when done.

## Open

Ordered, top first. A session takes the top item unless the launcher
names one. A row carries the intent, the mode and tier as routed, and
the owner when a session is on it. Nothing at the top may block on an
open question.

1. First-build lock-in: replace every `set at first build` deferral in
   docs/LOCKBOOK.md with a ruled value, and set the five QC gate
   commands for the static export stack. Mode standard, tier R0 as
   routed, no factor active; standard rather than express because the
   design-system and gate decisions sit in the durable band. Owner:
   none yet.

## Questions for the operator

One per line, dated when added; where a guard verdict raised the
question, name it (require-approval or manual-only). Cleared into the
brief or lock-book when answered.

- 2026-08-03: the domain is not yet owned. Which domain name should be
  registered, and by whom?
- 2026-08-03 (manual-only): the guard adapter named in docs/policy.json
  has no validation report, so every guarded class is manual-only. Do
  you want the adapter mapping written and the bypass suite run, or do
  you want to keep handing guarded actions yourself?

## Log

One line per finished Standard task, newest first: date, intent,
ruling, outcome. Express work lives in git history alone.

- (none yet)

# Recorded responses

`kestrel_authorisation.json` is the body the Kestrel sandbox returned
for a successful authorisation on 11 February 2026, captured with
`curl` and pasted in unchanged. Keep it that way: if the API changes,
recapture it rather than editing it by hand.

name: ci

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: [self-hosted, linux]
    steps:
      - uses: actions/checkout@v4
      - name: tests
        run: make test
      - name: component inventory
        run: make scan
      - name: byte compile
        run: make lint

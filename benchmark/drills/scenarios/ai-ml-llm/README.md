# Support ticket classifier

`classify(text)` puts a ticket in one of billing, bug, account or feature.
`labelled.jsonl` is our annotated sample; `second_annotator` is a second
pass over the same tickets and it does not always agree with the first.

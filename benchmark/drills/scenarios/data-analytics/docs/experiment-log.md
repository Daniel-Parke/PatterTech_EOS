# Variant test: checkout button copy

- Started 1 May 2026 and planned to run for twenty days.
- Traffic is split 50/50 between variant A (the current copy) and
  variant B (the new copy).
- A user is assigned once, on first visit, and the assignment is
  written to `raw/experiment.csv`.
- `converted` is 1 when that user completed a checkout inside the
  window, 0 otherwise.
- A mid-run check was taken on 8 May. Whatever came out of it was not
  written down.

Question still open for whoever picks this up: did variant B do better?

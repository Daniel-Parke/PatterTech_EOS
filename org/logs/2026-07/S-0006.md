---
summary: Session S-0006, Phase C item C1, the compile rules and walk order, dry run recorded
type: org
tags: [eos]
---

# S-0006 · 2026-07-07 · C1 compile rules and walk order

- role: build (entry mode 2)
- model: Claude Fable 5 in Claude Code
- launcher: Daniel, "work through all outstanding tasks and phases"

## What happened

Wrote inception/COMPILE.md (inputs enumerated and closed; prune, fill,
front-matter rewrite, assemble, distil, report; the compiler's
never-list including the protected-text rule and three strikes) and
inception/WALK_ORDER.md (trigger-filtered walk from the wargame index;
canonical order EOS scale, repo shape, voice, architecture, web,
delivery, ops; argued against inherited rules; the WG-DRAFT escape for
uncovered forks; the twenty-ruling budget alarm that re-runs the scale
ruling).

Dry run on paper against the B4 fixture seed: prune and fill reconcile
with the fixture's files, the ancestry table covers exactly the
matrix's S column minus the report itself, distillations correctly
empty for an all-inherited S walk, and the gate sequence matches what
S-0005 actually ran.

## Decisions taken

- Voice walks third, before architecture and web, because the register
  ruling colours every surface written after it.
- WG-EOS-001 and WG-EOS-002 always walk; the budget alarm at twenty
  rulings forces a scale re-argument rather than a longer walk.
- Draft wargames rule as WG-DRAFT-NNN in the lock-book, renamed by the
  harvest; keeps the rulings grammar parseable without blessing venture
  IDs into module namespaces.
- Add-on authoring is the one sanctioned compiler authoring, bounded to
  the add-on files and marked authored in the report.

## Noticed (dry-run findings)

- The fixture's slot and fence counts in its ancestry table are
  illustrative, not exact; the seed check does not verify counts.
  Acceptable for a fixture; a real compile counts honestly.
- The fixture lock-book carries no WG-EOS-002 or WG-VOX-001 rows, which
  a real walk would require. The fixture stays minimal on purpose; the
  E2 drill exercises the full walk.
- Compiled files trace through the report's ancestry table; a
  compiled_from front-matter key is convention, not mechanically
  enforced. Two enforcement mechanisms would drift; the report is the
  one of record.

## Handoff

Next: C2 (the voice module: doctrine, banned-list pattern, WG-VOX-001).

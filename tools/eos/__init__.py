"""EOS tooling package. Invoked as python -m tools.eos <cmd>."""

# Kept in step with tools/pyproject.toml by hand, and by a test, because
# a package that reports one version while its metadata reports another
# is worse than a package that reports none.
__version__ = "0.4.0"
